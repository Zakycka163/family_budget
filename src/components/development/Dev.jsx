import React from "react";
import PageTitle from "../PageTitle";

const Dev = (props) => {
    return (
        <div>
            <PageTitle title={props.title} />
            test
        </div>
    )
}

export default Dev;